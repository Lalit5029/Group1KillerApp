# This will Ignore peer dependency conflicts on install.
# Lets this project install without failing on outdated peer ranges (e.g. React 19 vs libs that only list React 18).
# This app will still run; peer deps are usually "works with" not "only works with".
legacy-peer-deps=true
